import { EntityModel } from "./entity.model";

export class CityModel extends EntityModel {
    CTCreated: Date;
    CTId: string;
    CTIdDepartamento: string;
    CTModified: Date;
    CTNombre: string;
}